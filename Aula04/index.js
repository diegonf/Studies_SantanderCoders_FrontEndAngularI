/*
  Models
    Criação de models e utilização no lugar de Interfaces
  Diretivas
    ngIg, ngClass, ngFor, ngModel ...
    É possível criar diretrizes personalizadas (prox aula)
  Data Binding
    Interpolação -> {{ }}
    Property binding -> [input]="input"
    Event binding -> (click)='funcaoOnClick'
    Two Way binding -> [(ngModel)]='variavel'
      Se alterar a variavel, altera o valor do input, se alterar o valor do input, altera a variavel. Ou seja, os dados vão nos dois 'caminhos'
*/